package com.example.loginform.Model

class Cake(id: Int,name: String, price: Int,availability: Boolean,image:Int) : Item("coffee") {
    private val id: Int = id
    private val name: String = name
    private val price: Int = price
    private val availability: Boolean = availability
    private val image: Int = image
}
